// const button = document.querySelector('button')
// const textList = ["NO!", "STOP!", "GO AWAY!"]

// button.addEventListener('mouseenter', function () {
// })

// button.addEventListener("mouseenter", () => {
//     // Sayfa genişliği ve yüksekliği
//     const pageWidth = window.innerWidth;
//     const pageHeight = window.innerHeight;

//     // Rastgele koordinatlar (elementin boyutunu hesaba katmak için offset düşülüyor)
//     const randomX = Math.floor(Math.random() * (pageWidth - button.offsetWidth));
//     const randomY = Math.floor(Math.random() * (pageHeight - button.offsetHeight));

//     // Yeni konumu ayarlayın
//     button.style.position = "absolute";
//     button.style.left = `${randomX}px`;
//     button.style.top = `${randomY}px`;
// });


